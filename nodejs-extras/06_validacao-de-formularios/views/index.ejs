<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style.css">
    <title>Document</title>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="card-header">
                <h1>Cadastro</h1>
            </div>
            <% if (emailError) { %>
                <span class="alert alert-danger"><%= emailError %></span>
            <% } %>
            <% if (nomeError) { %>
                <span class="alert alert-danger"><%= nomeError %></span>
            <% } %>
            <% if (pontosError) { %>
                <span class="alert alert-danger"><%= pontosError %></span>
            <% } %>
            <div class="card-body">
                <form method="POST" action="/form">
                    <input type="email" class="form-control" name="email" id="email" placeholder="E-mail" value="<%= email %>">
                    <br>
                    <input type="text" class="form-control" name="nome" id="nome" placeholder="Nome" value="<%= nome %>">
                    <br>
                    <input type="number" class="form-control" name="pontos" id="pontos" placeholder="Pontos" value="<%= pontos %>">
                    <br>
                    <input type="submit" value="Enviar">
                </form>
            </div>
        </div>
    </div>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</html>