import { Request, Response, NextFunction } from 'express';
declare global {
    namespace Express {
        interface Response {
            flash: (type: string, message: string) => void;
        }
    }
}
type OnConsumeFlash = (type: string, messages: string[]) => void;
type onAddFlash = (type: string, message: string) => void;
export interface FlashOption {
    sessionKeyName: string;
    onAddFlash?: onAddFlash;
    onConsumeFlash?: OnConsumeFlash;
}
export declare const getFlashMessages: (req: Request, sessionKeyName: string, type: string, onConsumeFlash?: OnConsumeFlash) => any;
declare const expressFlashMessage: (option: FlashOption) => (req: Request, res: Response, next: NextFunction) => Promise<void>;
export default expressFlashMessage;
