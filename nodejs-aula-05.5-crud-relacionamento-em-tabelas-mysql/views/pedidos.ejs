<%- include ('partials/header.ejs') %>
    <%- include ('partials/navbar.ejs') %>

        <div class="container">
            <div class="card">
                <div class="card-header">
                    <h1>Registrar pedido</h1>
                </div>
                <div class="card-body">
                    <form method="POST" action="/pedidos/new">
                        <label>Número do pedido:</label>
                        <input type="text" name="numero" id="numero" placeholder="Insira o número do pedido"
                            class="form-control" required>

                        <label>Valor do pedido:</label>
                        <input type="text" name="valor" id="valor" placeholder="Insira o valor do pedido"
                            class="form-control" required>

                        <label>Cliente:</label>
                        <input type="text" name="clienteId" id="clienteId" placeholder="Insira o ID do cliente"
                            class="form-control" required>
                        <br>
                        <button class="btn btn-success">Cadastrar</button>
                    </form>
                    <hr>
                </div>
            </div>
        </div>
        <br>

        <div class="container">
            <div class="card">
                <div class="card-header">
                    <h1>Pedidos</h1>
                </div>
                <div class="card-body">
                    <table>
                        <thead>
                            <tr>
                                <th>Número:</th>
                                <th>Valor:</th>
                                <th>Cliente:</th>
                                <th>Ações:</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% pedidos.forEach(pedido=> { %>
                                <tr>
                                    <td>
                                        <%= pedido.numero %>
                                    </td>
                                    <td>
                                        <%= pedido.valor.toLocaleString('pt-br', { style: "currency" , currency: "BRL"
                                            }) %>
                                    </td>
                                    <td>
                                        <%= pedido.cliente.nome %>
                                    </td>
                                    <td><a style="color: red;" href="/pedidos/pedido/<%= pedido.id %>">Excluir</a></td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <%- include ('partials/footer.ejs') %>